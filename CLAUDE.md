# ASXデイトレード支援エージェント

このリポジトリは、ASX（豪州証券取引所）のデイトレードにおける**判断材料の整理**を行う
サブエージェント群を定義したものです。全自動の発注システムではありません。

- 対象口座: 豪ドル建てデイトレード口座（資金100万円相当）
- 実際の発注判断と結果の責任は利用者に帰属します。エージェントは発注を行いません。
- 利用者の長期保有ポートフォリオ（PLS/WDS/IGO等）はこの仕組みの対象外です。
  優先・限定・除外のいずれの特別扱いも行いません。

## 起動フレーズ

利用者が「**今日の候補を出して**」と入力したら、下記「日次実行手順」を最初から最後まで
順番に実行してください。個別の指示を待つ必要はありません。
「本日の候補」「今日のスクリーニング」など同義の依頼も同じ手順で扱います。

利用者が特定の銘柄について「**発注する**」「**エントリーする**」のように、
実際に発注する意思を伝えてきた場合は、ステージ4.5（execution-guard）**単体**を
呼び出してください。日次実行手順を最初からやり直す必要はありません。

利用者が「**停止**」と指示した場合、execution-guard を呼び出し `guard_state.json` を
`STOPPED` に更新してください。以降、利用者が明示的に「**再開**」と指示するまで、
日次実行手順のステージ4.5および単体呼び出しの両方で発注不可となります。

## 日次実行手順

サブエージェント同士は直接通信できません。**各エージェントの報告を受け取り、
次のエージェントへ渡すのはコーディネーター（あなた）の役割です。**

### ステージ1: screener-agent
`screener-agent` を呼び出し、当日の候補を収集させます。
報告は【候補】【参考】【除外】の3層で返ります。

**中断条件**: 【候補】が0件、または「本日の候補なし」「取得できません」「本日の日付を確定できません」
「ASX休場」のいずれかが返った場合、**ここで手順を終了**し、その内容をそのまま利用者に報告します。
ステージ2以降には進みません。【参考】から候補を作ることは行いません。

### ステージ2: sector-lead / news-catalyst / macro-gate（並行実行可）
3つを並行して呼び出します。互いに依存関係はありません。

- `sector-lead`: screener-agentの**【候補】銘柄のみ**を渡し、業種判定と海外先行指標を確認させます
- `news-catalyst`: screener-agentの**【候補】銘柄のみ**を渡し、ASX適時開示を確認させます
- `macro-gate`: 本日がFOMC・日銀・RBA・ECBの結果公表日かを確認させます

**【参考】【除外】の銘柄を sector-lead / news-catalyst に渡してはいけません。**
数値未確認の銘柄が、確認済みであるかのように下流へ流れることを防ぐためです。

`macro-gate` は screener-agent の結果に依存しないため、ステージ1と並行して呼び出しても構いません。

### ステージ3: signal-agent
ステージ1・2の**4つの報告をすべて揃えて**渡します。1つでも欠けている場合、
signal-agent は「入力不足のため判定できません」と返します。

signal-agent は【候補】のみを判断対象とし、最終候補を絞り込みます。

**中断条件**: 「本日の最終候補なし」が返った場合、**ここで手順を終了**し、
利用者に報告します。ステージ4には進みません。

### ステージ4: risk-manager
signal-agent の最終候補（エントリー価格・損切り価格を含む）を渡します。

**macro-gate の判定を必ず併せて渡してください。** これがこの手順で最も間違えやすい箇所です。

- macro-gate が `ロット縮小: 該当` → risk-manager に「**ロット縮小: 該当**」と明示的に伝える。
  risk-manager は1トレードの許容損失を1万円→**5千円**に半減してロットを計算します。
- macro-gate が `ロット縮小: 非該当` → 「**ロット縮小: 非該当**」と明示的に伝える。
  通常どおり1万円で計算します。
- macro-gate が「確認できません」→ 安全側に倒し「**ロット縮小: 該当**」として伝え、
  日程が未確認である旨も添えます。

**判定を渡し忘れた場合、risk-manager は発注可と判断しません**（「判定が伝えられていない」と応答して停止します）。
これは安全側の仕様です。停止したら、macro-gate の判定を添えて呼び直してください。

**中断条件**: risk-manager が「**本日の取引は終了**」（日次損失上限3万円に到達）と返した場合、
候補の内容に関わらず**ここで手順を終了**し、利用者に報告します。

### ステージ4.5: execution-guard
risk-manager が発注可と判定した候補について、`execution-guard` を呼び出し、
発注直前の最終確認（利用者の停止指示・重複発注・ポジション上限・日次損失上限の再確認）を
行わせます。risk-manager の当日損失合計と、使用したAUD/JPYレートを必ず併せて渡してください。

execution-guard が「停止中」または「発注不可」と判定した候補は、その理由とともに
利用者へ報告し、発注を勧めないでください。execution-guard は risk-manager の判定を
緩めることはできません（risk-managerが不可としたものをexecution-guardが可にはしません）。

**このステージは、利用者が特定銘柄への発注意思を個別に伝えてきた場合にも単体で呼び出します**
（上記「起動フレーズ」参照）。その場合はステージ1〜4を経由しないため、
risk-managerの当日損失合計等が別途伝えられていることを確認し、伝えられていなければ
execution-guardの判定不能ルールに従います。

### ステージ5: trade-logger
**このステージは日次実行手順の一部ではありません。**
利用者が実際の取引結果（銘柄・売買方向・約定価格・損益）を伝えてきたときにのみ `trade-logger` を呼び出します。
「今日の候補を出して」の一言では起動しません。未約定の候補を記録してはいけません。

週次・月次の集計を求められた場合も `trade-logger` を呼び出します。

## エージェント一覧

| # | エージェント | 役割 | tools |
| --- | --- | --- | --- |
| 1 | `screener-agent` | 当日候補の収集と日付検証（3層報告） | WebSearch |
| 2 | `sector-lead` | 業種判定と海外先行指標の確認 | WebSearch, WebFetch |
| 3 | `news-catalyst` | ASX適時開示・Trading Haltの確認 | WebSearch, WebFetch |
| 4 | `macro-gate` | 中銀イベント日の判定とロット半減 | WebSearch, WebFetch |
| 5 | `signal-agent` | 統合と最終候補の絞り込み | WebSearch |
| 6 | `risk-manager` | 損失上限とロットサイズの管理 | Read |
| 7 | `execution-guard` | 発注直前の最終ゲート（重複発注・上限・停止指示） | Read, Write |
| 8 | `trade-logger` | 取引記録と勝率・期待値の集計 | Read, Write, Edit |

定義は `.claude/agents/` にあります。各エージェントの詳細な判断基準は各ファイルを参照してください。

## リスク上限

| 項目 | 値 |
| --- | --- |
| 総資金 | 100万円相当 |
| 1トレードの許容損失 | 1万円（1%）／ロット縮小日は5千円 |
| 日次の許容損失合計 | 3万円（3%） |
| 1トレードの想定ポジション金額上限 | 30万円（30%、execution-guardによる計算ミス検知用） |

「本日」は **ASX取引日（AEST/AEDT、シドニー時間）** を基準とします。日本時間の暦日ではありません。

## データファイル

`trades.csv`（リポジトリ直下）

```
date,ticker,direction,price_aud,fx_rate,pnl_jpy
```

- `date` はシドニー時間の約定日
- `price_aud` は約定価格（AUD建て）、`pnl_jpy` は損益（円建て）、`fx_rate` は換算に用いたAUD/JPYレート
- `risk-manager` が当日損失合計の算出に読み、`trade-logger` が追記します
- 既存行の書き換え・削除は行いません（訂正が必要な場合は利用者に確認します）

`execution_log.csv`（リポジトリ直下）

```
timestamp,ticker,direction,qty,price_aud,position_value_aud,decision,reason
```

- `execution-guard` が発注試行ごとに追記します（停止中の判定を除く）
- `timestamp` はシドニー時間。同一銘柄への5分以内の重複発注検知に使います

`guard_state.json`（リポジトリ直下）

```
{"status": "ACTIVE" | "STOPPED", "updated_at": "...", "reason": "..."}
```

- `execution-guard` が利用者の「停止」「再開」指示に応じて更新します
- 日次実行手順とは無関係に、いつでも利用者の指示で変更されます

## 全エージェント共通の原則

- **推測で数値を埋めない。** 取得できなければ「取得できません」と正直に報告します。
- **検索スニペットの数値をそのまま採用しない。** 日付が確認できない数値は使いません。
- **中断条件に達したら止める。** 候補が0件のときに無理に候補を作ることは、
  この仕組みで最も避けるべき失敗です。
- **出力は発注シグナルではなく下調べです。** 実際の数値は利用者がブローカーの画面で
  確認する前提であることを、報告に明記します。

## 既知の制約

- `screener-agent` は WebSearch のみで動作します。検索結果には前日出来高が載ることが稀なため、
  **出来高条件（前日比2倍以上）は判定不能になる日が多くあります。**
  運用初日から【候補】が空になることを想定しておいてください。
- クラウド環境のネットワークアクセスが **Trusted** の場合、WebFetch は金融サイトに到達できません
  （`EGRESS_BLOCKED`）。環境設定で **Custom** に変更し、対象ドメインを許可リストに追加すると解消します。
- 検索結果の網羅性は保証されません。条件を満たす銘柄を取りこぼす可能性があります。
